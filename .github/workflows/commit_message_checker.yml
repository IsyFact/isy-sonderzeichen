name: Commit Message Checker
on:
  pull_request:
    branches:
      - master
      - 'feature/**'

jobs:
  check-commit-message:
    uses: IsyFact/isyfact-standards/.github/workflows/commit_message_checker_template.yml@master